node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

/prisma/dev.db
# Backups of .env made by tooling — these contain the same secrets.
.env.backup-*
.env.*.backup
