# Copy to .env.local.
# Everything here is NEXT_PUBLIC_ and therefore visible in the browser bundle —
# never put a secret in this file.

NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# devnet | testnet | mainnet-beta. This is the single source of truth for which
# cluster the app talks to.
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Optional custom RPC. Falls back to the public cluster endpoint.
NEXT_PUBLIC_RPC_URL=

# Must match the backend's PLATFORM_WALLET_ADDRESS, or funding transactions will
# be rejected as sent to the wrong recipient.
NEXT_PUBLIC_PLATFORM_WALLET_ADDRESS=

# Public base URL for task images, served from Cloudflare R2. Must match the
# backend's R2_PUBLIC_URL: the backend builds image URLs from its own copy, and
# a mismatch shows as images that load in one place and 404 in another.
NEXT_PUBLIC_CDN_URL=https://pub-<hash>.r2.dev/

# Superseded by NEXT_PUBLIC_CDN_URL, still read as a fallback during the move
# off CloudFront. Safe to delete once NEXT_PUBLIC_CDN_URL is set everywhere.
# NEXT_PUBLIC_CLOUDFRONT_URL=
